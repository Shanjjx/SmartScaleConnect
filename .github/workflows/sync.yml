name: Sync

on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Show scaleconnect help
        run: |
          docker run --rm --entrypoint /bin/sh alexxit/smartscaleconnect:latest -c "scaleconnect -h || scaleconnect --help || true"
