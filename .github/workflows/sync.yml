name: Sync

on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Inspect run.sh
        run: |
          docker run --rm --entrypoint /bin/sh alexxit/smartscaleconnect:latest -c "cat /run.sh"
