name: Sync

on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Inspect container structure
        run: |
          docker run --rm --entrypoint /bin/sh alexxit/smartscaleconnect:latest -c 'ls -la /; find / -maxdepth 3 -name "*scale*" 2>/dev/null'
